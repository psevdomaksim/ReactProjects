import React from "react"
import "./MyButton.css"

const MyButton = (props) => {
    return(
        <button className={props.className}>
          
        </button>
    )

}

export default MyButton